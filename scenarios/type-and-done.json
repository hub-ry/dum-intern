{
  "name": "type-and-done",
  "why": "Answer every question with \"type it\", type a correct median into the hole, say done. The review should pass quietly and the skill should land.",
  "request": "add a median function to stats.py",
  "files": { "stats.py": "def mean(xs):\n    return sum(xs) / len(xs)\n" },
  "answer": "type it",
  "type": "    s = sorted(xs)\n    n = len(s)\n    mid = n // 2\n    return s[mid] if n % 2 else (s[mid - 1] + s[mid]) / 2",
  "expect": { "maxQuestions": 2, "minHoles": 1, "reviewPasses": true, "maxReplyLines": 6 }
}
